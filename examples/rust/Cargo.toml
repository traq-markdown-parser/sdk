[package]
name = "traq-markdown-example"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
traq-markdown-processor = { path = "../../crates/processor" }
traq-markdown-grammar = { path = "../../crates/grammar" }
